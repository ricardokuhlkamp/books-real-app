{
  "version": 2,
  "builds": [
    { "src": "*.js", "use": "@vercel/node" }
  ],
  "routes": [
    {
      "srcroutes": "/book/(.*)",
      "dest": "/book/$1"
    },
    {
      "src/routes": "/user/(.*)",
      "dest": "/user/$1"
    }
  ]
}
