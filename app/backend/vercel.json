{
  "version": 2,
  "builds": [
    { "src": "*.js", "use": "@vercel/node" }
  ],
  "routes": [
    {
      "src": "/book/(.*)",
      "dest": "/book/$1"
    },
    {
      "src": "/user/(.*)",
      "dest": "/user/$1"
    }
  ]
}
