{
  "version": 2,
  "builds": [
    { "backend": "*.js", "use": "@vercel/node" }
  ],
  "routes": [
    {
      "backend/routes": "/book/(.*)",
      "dest": "/book/$1"
    },
    {
      "backend/routes": "/user/(.*)",
      "dest": "/user/$1"
    }
  ]
}
